namespace VoiceCtrl.Hub;

/// <summary>The five screens listed in the Hub's sidebar, in nav order.</summary>
public enum HubSection
{
    Dictations,
    Dictionary,
    Snippets,
    Profiles,
    Settings,
}
